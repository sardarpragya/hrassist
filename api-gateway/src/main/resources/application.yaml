spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:2002
          predicates:
            - Path=/api/users, /api/users/**

server:
  port: 2001
